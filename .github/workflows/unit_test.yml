name: Unit Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        java-version: [ 11 ]

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v2
      with:
        java-version: ${{ matrix.java-version }}
        distribution: adopt
    - name: Run unit tests and assemble APKs
      uses: gradle/gradle-build-action@v2
      with:
        arguments: assembleProdRelease testProdReleaseUnitTest
    - name: Check Output
      run: bash ./etc/CheckBuild/check-release.sh test
    - name: Get current date
      id: get_date
      run: echo "RELEASE_DATE=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV
    - uses: NoCrypt/sign-android@main
      name: Sign app APK
      id: sign_app
      with:
        releaseDir: ${{ secrets.ANDROID_RELEASE_DIR }}
        signingKey: ${{ secrets.ANDROID_SIGNING_KEY }}
        keyAlias: ${{ secrets.ANDROID_KEY_ALIAS }}
        keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
        appName: ${{ secrets.ANDROID_APP_NAME }}
        appPrefix: ${{ secrets.ANDROID_APP_PREFIX }}
    - name: Example Release
      uses: 'marvinpinto/action-automatic-releases@latest'
      with:
        repo_token: '${{ secrets.GITHUB_TOKEN }}'
        automatic_release_tag: 'latest'
        prerelease: true
        title: "Nightly build ${{ env.RELEASE_DATE }}"
        files: |
          ${{steps.sign_app.outputs.signedFile}}
    #- name: Create Release
    #  id: create_release
    #  uses: actions/create-release@v1
    #  env:
    #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #  with:
    #    tag_name: ${{ env.RELEASE_DATE }}
    #    release_name: "Nightly build ${{ env.RELEASE_DATE }}"
    #    draft: false
    #    prerelease: true
    #- uses: ilharp/sign-android-release@v1 # Or use @nightly
    #  name: Sign APK
    #  id: sign_app
    #  with:
    #    releaseDir: ${{ secrets.ANDROID_RELEASE_DIR }}
    #    signingKey: ${{ secrets.ANDROID_SIGNING_KEY }}
    #    keyAlias: ${{ secrets.ANDROID_KEY_ALIAS }}
    #    keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
    #    keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
    #    buildToolsVersion: 35.0.0
    #- name: Upload APK
    #  uses: actions/upload-release-asset@v1
    #  env:
    #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #  with:
    #    upload_url: ${{ steps.create_release.outputs.upload_url }}
    #    asset_path: ${{steps.sign_app.outputs.signedFile}}
    #    asset_name: xDrip-780g-nightly-${{ env.RELEASE_DATE }}.apk
        asset_content_type: application/vnd.android.package-archive
